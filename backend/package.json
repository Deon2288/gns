{
  "name": "gns-backend",
  "version": "1.0.0",
  "description": "GNS Backend",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "avl": "node src/avl-server.js",
    "start:all": "concurrently \"npm run dev\" \"npm run avl\"",
    "migrate:simcontrol": "psql -U ${DB_USER:-postgres} -d ${DB_NAME:-gns_db} -f src/migrations/001_simcontrol.sql"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "concurrently": "^8.2.2",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.1",
    "express-rate-limit": "^8.3.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.20.0",
    "ping": "^0.4.4",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
