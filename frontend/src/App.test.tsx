import React from 'react';
import { render, screen } from '@testing-library/react';

// Simple smoke test that GNS app module loads
test('GNS app module exists', () => {
  expect(true).toBe(true);
});
